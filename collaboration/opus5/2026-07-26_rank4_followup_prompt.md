# Opus 5 xhigh: next two-coordinate frontier

The Grassmann-line ratio theorem has now survived both the root audit and your
independent audit.  Continue the mathematics at the exact next boundary.

Let \(P,Q\) be two unrelated decomposable \(k\)-forms over
\(\mathbb F_p\), \(k=p-1\), and colour a \(k\)-set by
\[
 h([P(S):Q(S)])
\]
for an arbitrary decoder \(h:\mathbb P^1(\mathbb F_p)\to\mathbb F_p\).
After contraction at a \((k-2)\)-face, the two alternating 2-forms can span a
pencil containing rank-four members; the oval/collinear-centre proof no
longer applies.

Do the heavy lifting on this rank-four case:

1. classify a pair of alternating 2-forms under congruence and projective
   change of pencil parameter, with attention to common radical and
   Pfaffian-root cases;
2. translate the \(K_{p+1}\) condition that every vertex sees \(p\) distinct
   decoded colours into exact geometry/combinatorics of that pencil;
3. try to prove a broader no-go at \(p=17\), or else construct an exact local
   positive link demonstrating the escape;
4. test every claimed universal statement at \(p=3,5,7\);
5. distinguish a local link from a global pair of Plücker systems.

Do not return a recap.  Export a self-contained proof or sharp frontier plus
dependency-free verifier under
`collaboration/opus5/rank4_plucker_ratio/`.  If you find only a local
positive control, say exactly what global compatibility is still missing.
Do not commit, and do not claim #835 solved unless you have a full global
construction or unrestricted theorem.
