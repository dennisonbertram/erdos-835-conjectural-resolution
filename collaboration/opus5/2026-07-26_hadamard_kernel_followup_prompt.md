# Opus 5 follow-up: unrestricted deleted-colour Hadamard kernel

We are still working on Erdős–Rosenfeld Problem #835.  Do not switch
problems and do not claim a solution without a complete rigorous proof.

Read, in this order:

1. `collaboration/hadamard_kernel_attack/README.md`
2. `collaboration/hadamard_kernel_attack/verify_hadamard_kernel_attack.py`
3. `erdos_835_conjectural_resolution.md`, especially Sections 7.3--7.4

First independently audit the deleted-colour theorem, including the
higher-degree converse and every dimension/count.  If it is wrong, give the
smallest explicit counterexample or exact broken inference.

If it is sound, do the heavy mathematical work on the actual open target:
for \(p=17,k=16\), prove or disprove that a zero class
\(D=S(15,16,32)\) can have a 15-dimensional
\[
U\le\ker M_D
\]
with
\[
M_D(x\circ y)\in\langle\mathbf1\rangle
\quad(x,y\in U)
\]
and nondegenerate scalar form.  A uniform upper bound below 15 would rule
out \(k=16\).  It is acceptable, and likely better, to descend rigorously
to a derived \(S(4,5,21)\) member and prove an inherited capacity bound
there, but you must prove that the descent preserves the required
15-dimensional structure.

Promising directions to investigate rigorously:

- modular representation theory and the \(p\)-rank/module structure of the
  deleted inclusion matrix;
- polarization and the algebra generated by coordinatewise products;
- the local simplex Gram frames and compatibility across overlapping stars;
- higher-power identities forced by the same \(U\);
- descent to derived large sets with an exact injectivity argument;
- an absolute or coding-theoretic bound for a totally
  constant-Hadamard-product subspace.

Do not assume symmetry of \(D\), do not replace an arbitrary Steiner system
by a known Witt/Wallis system without proof, and do not promote numerical
search to a theorem.  Check every proposed inequality against the true
\(k=2\) control and the exact false \(k=4\) control.

Write a self-contained note and any exact verifier under
`collaboration/opus5/hadamard_kernel_followup/`.  Label each item
PROVED, COMPUTATION, CONJECTURE, or FAILED ATTACK.  The preferred outcome is
a complete unrestricted contradiction at \(k=16\); otherwise return the
strongest rigorously proved reduction and the exact remaining gap.
