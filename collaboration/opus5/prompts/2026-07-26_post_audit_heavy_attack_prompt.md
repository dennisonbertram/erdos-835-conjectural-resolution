# Post-audit heavy attack: pursue an actual decisive theorem

Thank you for correcting the staircase note.  Continue on Erdős--Rosenfeld
#835; do not stop at the corrected audit and do not spend the next pass merely
cataloguing known identities.

Current certified state:

- A hypothetical \(k=16\) cover is exactly an
  \(LS(14,15,31)\); every constituent \(S(14,15,31)\) is automatically
  intersecting.
- The all-\(k\) H1/H2 packages now prove the complete scalar actions and
  show that all ordinary scalar divisibilities automatically pass whenever
  \(k+1\) is prime.
- The staircase support is valid, but its compression consequence is only
  \(\dim {\cal J}P_a\le\max(1,a-1)\); exact rank is known at \(a\le3\).
- The first state-sensitive H3 data are the low-rank integer skew commutator
  \(\Delta\) and its coupled commutator
  \(R\Delta+\Delta R-5\Delta\).
- A necessary shadow is \(LS(3,4,20)\); exact solvers are still running and
  have no verdict.

Use Opus-level effort to pursue one genuinely decisive route.  Choose the
route with the best chance after inspecting the committed work, but a useful
priority order is:

1. a general prime-parameter obstruction from the top-harmonic/inclusion
   lattice, Smith normal form, characteristic-polynomial congruences, or
   higher-module integrality that goes beyond scalar H1/H2;
2. a rigorous construction of \(LS(14,15,31)\), possibly by an explicit
   group-orbit/difference-family or multi-coordinate finite-field rule, with
   a complete checker;
3. a human-scale reduction or proof certificate excluding
   \(LS(3,4,20)\), which would at least exclude the first open case.

Requirements:

- Read the relevant committed artifacts before deriving, so the attack does
  not duplicate a closed route.
- State one sharply formulated target lemma first.
- Try hard to prove or refute that lemma.  If it fails, exhibit a concrete
  countermodel/certificate and move immediately to the next strongest lemma.
- Any computation must be exact and independently checkable.
- Write the result under a new
  `collaboration/opus5/<descriptive_frontier>/` directory with a proof note
  and verifier.
- A restricted ansatz result must be labelled restricted.  Do not claim
  #835 or \(k=16\) solved without a complete construction or contradiction.

The goal is a new decisive mathematical step, not a status summary.  Keep
working until you have either a valid theorem/certificate or a precisely
isolated finite obstruction that materially advances the unrestricted case.
