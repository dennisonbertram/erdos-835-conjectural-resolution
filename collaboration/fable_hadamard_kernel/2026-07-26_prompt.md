# Fable follow-up: derived-layer Hadamard capacity

Remain on Erdős–Rosenfeld Problem #835.

Read:

- `collaboration/hadamard_kernel_attack/README.md`
- `collaboration/hadamard_kernel_attack/verify_hadamard_kernel_attack.py`
- `erdos_835_conjectural_resolution.md`, Sections 7.3--7.4

Independently check the new deleted-colour Hadamard-kernel theorem.  Then
attack its smallest rigorous \(k=16\) shadow: derive the forced
15-dimensional quadratic space through the appropriate
\(LS(4,5,21)\) or \(LS(3,4,20)\) layer, and try to prove that no member can
support it.

Focus on exact combinatorial/module reductions that an independent verifier
can check.  In particular, determine whether repeated derivation preserves
the full dimension and nondegenerate scalar pairing, whether the local
\(I+J\) simplex frames force an impossible overlap Gram matrix, and whether
the modular inclusion-matrix rank gives a universal capacity bound.

Do not assume a classified or symmetric Steiner system.  Treat computation
only as computation.  A negative \(k=16\) theorem must cover every possible
zero class; a result for one design is only a control.

Place a coherent note and exact scripts under
`collaboration/fable_hadamard_kernel/`.  State explicitly whether #835 or
even \(k=16\) is resolved.
