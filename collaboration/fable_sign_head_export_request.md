# Export the corrected sign-head evidence

Please let the sign-head subagent finish.  Then export into the repository:

1. the proof that \(E(L,M)\equiv+1\) for every admissible even-\(k\) chart;
2. every claimed one-sided synthetic table witnessing both signs, with an
   assertion-enabled stdlib verifier and explicit statement of exactly which
   per-\(ij\), per-\(uv\), palette, and shared-\(N\) constraints it satisfies;
3. a corrected theorem/no-go note that never treats `H_required` on a
   nonextendible radius-3 chart as an observed \(H\);
4. raw counts and hashes.

Do not rely on session memory alone.  Put the reproducible artifacts under
`collaboration/fable_sign_head/` or `evidence/odd_graph_local_ball/`, leave
them uncommitted, and report the paths when frozen.
